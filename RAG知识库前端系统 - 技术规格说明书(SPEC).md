# RAG知识库前端系统 \- 技术规格说明书\(SPEC\)

## 1\. 文档信息

- **项目名称**：RAG 智能知识库前端管理系统

- **客户端类型**：Web 端（PC 后台管理系统）

- **技术栈**：Vite 8 \+ React19 \+ TypeScript 6 \+ TailwindCSS 4 \+ ShadCN UI、包管理器：pnpm

- **状态管理**：Zustand \+ TanStack React Query

- **网络请求**：Axios \+ SSE 流式问答

- **适用场景**：用户登录认证、文档素材管理、向量数据管理、结构化数据 CRUD、RAG 智能问答

- **文档用途**：前端开发规范、接口定义、功能边界、交互逻辑、工程结构标准、UI 设计风格

- **UI 风格**：对标 open-webui / ChatGPT 现代极简对话式风格（详见第 4 章）

## 2\. 项目整体概述

### 2\.1 系统目标

搭建一套轻量化、现代化的 RAG 知识库前端控制台，面向普通用户提供：账号登录与个人信息管理、文档素材上传与管理、文档向量化任务管理、结构化数据维护、智能问答对话能力，完整承接后端 RAG 服务能力，实现知识库可视化运维与智能问答交互。

### 2\.2 核心能力

- 用户体系：登录、个人信息查看、密码修改、登录态持久化

- 文档管理：文档新增、编辑、删除、上传、列表分页、状态查看

- 向量任务：触发文档向量化、异步任务进度监听、失败重试

- 结构化数据：表格类结构化数据 CRUD 管理

- 智能问答：流式对话、知识库范围选择、溯源引用、会话管理

## 3\. 技术架构规范

### 3\.1 分层架构（强制遵循）

页面层 → 业务组件层 → 状态管理层 → 服务请求层 → 工具基础层

1. **页面层**：纯页面视图，不写请求、不写复杂逻辑

2. **业务组件层**：通用业务组件封装（上传、聊天、表格、任务进度）

3. **状态层**：全局用户状态\(Zustand\)、服务数据缓存\(React Query\)

4. **请求层**：统一 Axios 封装、TS 类型、拦截器、错误处理

5. **工具层**：文件处理、格式化、校验、本地存储、常量

### 3\.2 核心依赖版本规范（以项目实际package\.json为准）

- **构建工具**：Vite 8\.0\.16（最新正式版）

- **核心框架**：React 19\.2\.7 \+ TypeScript 6\.0\.3（超前稳定版本）

- **UI 样式体系**：ShadCN UI \+ TailwindCSS 4\.3\.0 \+ @tailwindcss/vite 4\.3\.0

- **路由方案**：react\-router\-dom 7\.15\.1

- **状态管理**：zustand 5\.0\.14

- **服务缓存**：@tanstack/react\-query 5\.x 最新稳定版（具体版本以 pnpm\-lock\.yaml 为准）

- **表单校验**：内置react\-hook\-form配套生态（工程已预留规范）

- **表格组件**：@tanstack/react\-table 8\.21\.3

- **消息通知**：sonner 2\.0\.7

- **图标库**：lucide\-react 1\.17\.0

- **Markdown渲染**：react\-markdown 10\.1\.0，支持公式/代码块/表格渲染

- **网络请求**：axios 1\.16\.1

- **文件上传**：react\-dropzone 15\.0\.0

- **异常处理**：react\-error\-boundary 6\.1\.2

- **数学公式**：katex 0\.17\.0、rehype\-katex、remark\-math

- **可视化拓展**：@react\-sigma 全量图谱可视化套件（预留知识库图谱功能）

- **检索工具**：minisearch 7\.2\.0（本地检索能力支撑）

- **废弃依赖清理**：完全移除 i18next / react\-i18next 国际化相关能力与配置，项目不做多语言适配

## 4\. UI 设计风格规范

### 4\.1 风格定位

整体 UI 风格对标 **open-webui / ChatGPT** 的现代极简对话式产品风格，强调大留白、弱边框、内容优先的视觉表达，弱化后台管理系统的"表单感"，营造轻量、沉浸、专注的问答与知识管理体验。

- **视觉基调**：极简克制，以中性灰阶为主色调，仅用少量品牌色点缀交互焦点

- **设计语言**：卡片化布局、大圆角、柔和阴影、无重边框（以 1px 浅描边或背景色区分层级）

- **信息密度**：适中偏低，行距宽松、段落留白充足，长文本阅读舒适

### 4\.2 色彩体系

- 主色调：中性灰阶体系，亮色模式以白色/浅灰为底，暗色模式以深灰近黑为底（对齐 open-webui 暗色体验）

- 品牌强调色：单一强调色（建议蓝紫色系），仅用于主按钮、链接、选中态与聚焦态，禁止大面积铺色

- 语义色：成功/警告/错误/信息色遵循 ShadCN 语义色板，用于任务状态 Badge 与消息提示

- 全部颜色统一收敛到 TailwindCSS 4 主题变量（CSS Variables）定义，禁止在组件中硬编码色值

### 4\.3 布局规范

- 主布局采用**左侧边栏 \+ 主内容区**结构：侧边栏可折叠收起，问答页侧边栏承载会话历史列表（ChatGPT 式）

- 内容区统一最大宽度约束（如正文内容不超过 768px 居中显示），避免宽屏下长行阅读疲劳

- 对话消息区采用单列居中流式布局：用户消息右对齐气泡或浅灰底色块，AI 回复左对齐全宽文本展示（ChatGPT 式）

- 管理列表页采用"顶部工具栏（搜索\+操作按钮）\+ 轻量卡片/表格"结构，表格使用无竖线、行悬浮高亮的现代样式

- 页面间留白、间距使用统一的 4px 基础栅格倍数，禁止随意间距

### 4\.4 字体与排版

- 字体：系统默认无衬线字体栈，优先原生系统字体，保证跨平台渲染一致

- 字号层级：页面标题 24px、模块标题 16\-18px、正文 14\-15px、辅助说明 12\-13px，行高不低于 1\.5

- Markdown 回答渲染区使用宽松排版：代码块带圆角背景与复制按钮，表格带浅色边框，公式居中渲染

### 4\.5 组件风格细则

- 按钮：圆角矩形，主按钮使用强调色，次按钮幽灵/描边样式，图标按钮保持简洁无文字

- 输入框：大圆角、浅描边，聚焦时强调色描边发光；问答输入框采用 ChatGPT 式底部悬浮大输入卡片（内嵌发送/停止按钮）

- 对话框/抽屉：统一使用 ShadCN Dialog/Sheet，圆角 12\-16px，遮罩半透明模糊

- 状态徽标：向量任务状态使用柔和底色 Badge（处理中带加载动效，成功绿、失败红、待处理灰）

- 图标：统一 lucide 线性图标，线宽一致，尺寸按 16/20/24px 分级

- 空状态：所有列表空态配插图式图标 \+ 引导文案 \+ 操作按钮，禁止裸文字空页

### 4\.6 动效与交互反馈

- 过渡动效时长统一 150\-300ms，使用 ease\-out 缓动，禁止弹跳类夸张动画

- 流式回答展示打字光标/加载点动效；请求中按钮展示 loading 且禁用

- 列表操作（删除、状态变更）使用轻提示 toast（sonner），位置统一右下角

- 侧边栏折叠、主题切换使用平滑过渡动画

### 4\.7 明暗主题要求

- 全站强制支持明/暗双主题，基于 ShadCN CSS 变量体系实现，一键切换并持久化用户偏好

- 暗色模式不是简单反色：背景使用深灰（非纯黑）、文本使用柔白、阴影弱化，对齐 open-webui 暗色观感

## 5\. 工程规范

### 5\.1 工程基础配置（包管理与脚本）

项目**强制唯一包管理器：pnpm**，完全禁止 npm / yarn / bun 安装依赖、运行脚本，保证团队依赖版本一致性、锁文件有效性，项目所有开发、构建、预览指令统一使用 pnpm。

- **标准开发**：pnpm dev

- **生产构建**：pnpm build

- **项目预览**：pnpm preview

- **代码校验**：pnpm lint

- **单元测试**：pnpm test / pnpm test:watch / pnpm test:coverage

### 5\.2 工程目录结构规范（强制标准）

```Plain Text
src
├── api/                 # 接口层（纯请求+类型）
│   ├── auth.ts          # 登录、用户、密码
│   ├── document.ts      # 文档CRUD、向量任务
│   ├── struct.ts        # 结构化数据CRUD
│   ├── chat.ts          # 问答会话、流式接口
│   └── types/index.ts   # 全局接口TS类型定义
├── assets/              # 静态资源
├── components/
│   ├── ui/              # 原生 shadcn 组件（不修改源码）
│   └── business/        # 自研业务组件
├── hooks/               # 自定义业务hooks
├── layout/              # 主布局（侧边栏+header）
├── pages/               # 所有页面
├── router/              # 路由配置 + 路由守卫
├── store/               # zustand 全局状态
├── utils/               # 工具函数
├── App.tsx
└── main.tsx

```

## 6\. 全局通用规范

### 6\.1 网络请求规范

- 统一 Axios 实例封装，统一 baseURL、超时时间

- 请求拦截：自动注入 Authorization: Bearer \{token\}

- 响应拦截：统一错误提示、401 自动登出并跳转登录

- 所有接口**强制编写完整 TS 入参/出参类型**，杜绝隐式 any、空类型定义

- 普通 CRUD 接口使用 Promise 异步请求；RAG 问答统一使用**基于 fetch \+ ReadableStream 的流式 SSE 封装**，不使用原生 EventSource（其不支持自定义 Authorization 请求头且仅支持 GET，无法携带 Bearer Token 与 POST 请求体）；封装要求：支持 POST 请求体与自定义请求头、AbortController 手动中止、超时销毁、异常捕获；流式问答**默认不自动重连**（自动重连会导致重复回答），断流后由用户手动重试

- SSE 鉴权统一走 Authorization 请求头，**禁止通过 URL query 传递 Token**

### 6\.2 鉴权规范

- 未登录用户禁止访问所有业务页面，自动跳转登录页

- 登录态、用户信息、Token 采用 **Zustand 状态 \+ localStorage 持久化**，页面刷新自动恢复登录态，过期 Token 自动清空

- 异常状态统一处理策略：**401** 清空本地状态并跳转登录页；**403** 保留登录态，仅提示「无权限」，不做登出

- 并发请求同时触发 401 时做去重处理：只清空一次状态、只提示一次、只跳转一次

### 6\.3 组件开发规范

- UI 通用组件全部使用 ShadCN 原生组件，不重复造轮子

- 业务功能统一封装至 business 组件，页面只做组合使用

- 所有组件、页面、接口、工具函数**必须全 TS 强类型**，禁止显式 any、隐式 any、空类型断言，开启严格TS校验

## 7\. 路由设计（页面清单）

|路由路径|页面名称|是否需要登录|
|---|---|---|
|/login|登录页|否|
|/user/profile|个人中心|是|
|/document/list|文档管理列表|是|
|/document/struct|结构化数据管理|是|
|/chat|RAG智能问答页|是|
|/|首页重定向至问答/文档页|是|

## 8\. 核心模块详细功能SPEC

### 8\.1 登录 \& 用户模块

#### 功能清单

- 账号密码登录，表单校验（非空、格式）

- 登录成功存储 Token、缓存用户信息

- 个人中心：展示用户名、账号、创建时间等信息

- 修改密码（旧密码\+新密码\+确认密码校验）

- 退出登录：清空状态、清空token、跳转登录

#### 状态设计

useUserStore：token、userInfo、isLogin

### 8\.2 文档向量管理模块（核心CRUD）

#### 基础功能

- 文档列表分页展示：标题、类型、创建时间、向量状态、任务状态

- 手动新建文本文档：输入标题\+正文

- 编辑已有文档内容

- 删除文档（二次确认）

- 文件上传：严格支持 TXT / MD / PDF，前端前置校验文件格式、文件大小、空文件拦截，禁止非法文件上传

#### 向量任务核心逻辑

- 支持对未向量化文档**手动触发向量化任务**

- 提交任务后返回 taskId，前端轮询任务状态

- 任务状态枚举严格约束：待处理 / 处理中 / 成功 / 失败，前端做状态枚举常量统一管理，禁止硬编码文本

- 失败任务支持**重试向量化**

- 任务成功后，文档标记为「已向量入库」，可用于问答召回

### 8\.3 结构化数据管理模块

针对 RAG 结构化知识库（表格数据、键值数据）提供完整 CRUD：

- 结构化数据列表分页

- 新增结构化条目

- 编辑条目字段

- 删除单条/批量删除

- 适配后端结构化向量入库逻辑

### 8\.4 RAG智能问答模块

#### 核心功能

- 知识库范围选择：可指定本次问答召回的文档范围

- 流式问答输出（SSE 实时打字效果）

- Markdown 渲染回答内容（代码块、表格、公式）

- 问答溯源：展示引用的文档名称、片段内容

- 新建会话、切换历史会话、清空会话

- 支持中止当前问答请求：通过 AbortController 主动中止流式请求、释放 reader、清空残留流式数据、锁定请求状态，防止内存泄漏与重复渲染

#### 交互规则

- 请求中全局锁状态，禁止重复发送提问、禁止并行多个SSE连接

- loading 状态展示

- 异常断连、网络超时、服务宕机统一捕获，自动销毁SSE实例，给出友好提示，防止页面卡死

## 9\. 接口通用规范

### 9\.1 通用返回结构

```Plain Text
{
  code: number      // 0 表示成功，非 0 为业务错误码
  message: string
  data: T
}

```

- **分页协议**：请求统一使用 page（从 1 开始）、pageSize 参数；响应统一为 \{ list: T\[\], total, page, pageSize \} 结构

- **资源定位协议**：单资源更新/删除统一走 path 参数（/xxx/\{id\}），禁止 body 传 id；批量操作使用独立的 batch 端点

### 9\.2 核心接口清单

#### 用户接口

- POST /api/auth/login 登录

- GET /api/auth/info 获取用户信息

- PUT /api/auth/password 修改密码

#### 文档接口

- GET /api/document/list?page=&pageSize=&keyword= 文档分页列表

- GET /api/document/\{id\} 文档详情

- POST /api/document 创建文档（body：\{ title, content, type \}，手动新建文本文档）

- PUT /api/document/\{id\} 更新文档（body：\{ title, content \}）

- DELETE /api/document/\{id\} 删除文档（若文档已入向量库，后端需联动删除向量数据）

- POST /api/document/upload 文件上传（multipart/form\-data，字段名 file；成功后返回新建的文档记录）

- GET /api/document/options 问答范围选择数据源：已入库文档列表（id \+ 标题）

- POST /api/vector/task/start 发起向量化任务（body：\{ documentId \}，返回 \{ taskId \}）

- GET /api/vector/task/status?taskId= 查询任务状态

- POST /api/vector/task/retry 失败任务重试（body：\{ taskId \}）

#### 结构化数据接口

- GET /api/struct/list?page=&pageSize= 结构化数据分页列表

- POST /api/struct 新增条目

- PUT /api/struct/\{id\} 编辑条目

- DELETE /api/struct/\{id\} 删除单条

- DELETE /api/struct/batch 批量删除（body：\{ ids: string\[\] \}）

- POST /api/struct/vectorize 触发结构化数据向量化入库（body：\{ id \}）

- 以上接口全部编写强类型 TS 入参/出参定义，禁止模糊接口定义

#### 问答接口

- SSE POST /api/chat/stream 流式问答（body：\{ sessionId, question, documentIds \}，事件协议见 9\.3）

- GET /api/chat/session/list 会话列表

- POST /api/chat/session/new 新建会话

- DELETE /api/chat/session 删除会话

### 9\.3 SSE 流式问答事件协议

响应 Content\-Type 为 text/event\-stream，按空行分隔事件，data 字段统一为 JSON，事件类型如下：

|事件类型|data 结构|说明|
|---|---|---|
|message|\{ content: string \}|增量文本片段，前端按序追加拼接渲染|
|references|\{ items: \[\{ documentId, title, snippet, score \}\] \}|溯源引用，回答结束前下发一次|
|done|\{ sessionId \}|流结束标志，收到后关闭连接并解锁请求状态|
|error|\{ code, message \}|业务异常，展示 message 并终止流|

- 解析容错：必须处理半截 JSON（chunk 可能切断事件行），使用缓冲区拼接完整事件后再解析

- 资源释放：收到 done/error 或用户中止后，必须取消请求并释放 ReadableStream reader，杜绝内存泄漏

- 鉴权：Authorization 请求头携带 Bearer Token，禁止 URL query 传参

## 10\. 状态管理设计

### 10\.1 全局状态（Zustand）

- **userStore**：统一管理 token、userInfo、isLogin 登录状态，提供 login / logout / updateUserInfo 方法，支持持久化、登录态重置

- **全局状态约束**：所有全局状态变更必须走 store 方法，禁止组件直接修改状态

### 10\.2 服务缓存（React Query）

- 文档列表：开启缓存、防抖、窗口聚焦自动刷新、请求节流

- 向量任务：固定时间轮询、轮询防抖、任务结束自动停止轮询（防内存泄漏）

- 问答会话：缓存会话列表、失效自动刷新、避免重复请求

- **React Query 全局配置规范**：统一过期时间、重试次数、请求防抖、错误回调，在入口统一初始化

## 11\. 异常处理规范

- 网络异常：统一 toast 提示「网络请求失败」

- 401：清空登录态，跳转登录

- 403：提示无权限

- 业务失败码：后端返回 message 前端直接展示

- 文件上传超限、格式错误：前端前置校验

- 问答断流、超时、服务异常：主动终止 SSE 链接、清空加载状态、销毁实例，杜绝内存泄漏

## 12\. 扩展性预留设计

- 预留多知识库维度字段，支持后续多知识库切换、知识库隔离问答

- 预留权限判断钩子，支持后续角色权限体系、按钮级权限控制

- SSE 封装支持手动中止、超时销毁、异常捕获，杜绝内存泄漏；流式问答默认不自动重连，避免重复回答（后续如需幂等重连须后端配合消息去重）

- 文件工具类预留分片上传、大文件断点续传扩展方法

- 全局主题适配 ShadCN 明暗主题切换，原生兼容TailwindCSS 4版本特性

- 工程**强制 pnpm 唯一依赖管理**，使用 pnpm\-lock\.yaml 锁定版本，团队环境完全一致

- 预留全局错误边界，配合 react\-error\-boundary 捕获组件渲染异常，防止页面白屏

## 13\. 开发约束（硬性规则）

1. 禁止出现 any、禁止隐式 any，所有数据强制类型定义

2. 所有接口统一在 api 层管理，页面不直接写请求

3. 所有通用逻辑抽离 hooks / utils，页面只做视图

4. UI 组件统一使用 shadcn，禁止自定义重复基础组件

5. 异步任务统一使用 React Query 轮询管理，禁止裸写定时器；完全废弃国际化相关代码与能力，全程使用 pnpm 执行所有工程指令。

> （注：部分内容可能由 AI 生成）
