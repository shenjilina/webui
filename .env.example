# 环境变量示例文件（复制为 .env.local 可覆盖任意变量）
# API 基础地址：开发环境使用 Vite 代理路径 /api，生产环境可为绝对地址
VITE_API_BASE_URL=/api
# 是否启用 MSW mock 服务（仅建议开发环境开启）
VITE_ENABLE_MOCK=true
# 应用标题
VITE_APP_TITLE=RAG 智能知识库
# SSE 流式超时时间（毫秒）
VITE_SSE_TIMEOUT=300000
